# Standard Local Status Page

The status page is built directly into the telemetry agent and is available at `/status` when `agent.status_page` is enabled.

It uses the same light/dark design system as the Community Dashboard, including the muted off-white light theme. Only telemetry enabled in the agent configuration is rendered. It is read-only and contains no configuration or control endpoints.
